generator client {
  provider = "prisma-client-js"
  output   = "../app/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model MiningStats {
  id    Int     @id @default(autoincrement())
  highest_pool_hashrate Int
}